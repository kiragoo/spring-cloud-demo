server:
  port: 8003
spring:
  application:
    name: microservice-hystrix-dashboard
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka/
  instance:
    prefer-ip-address: true